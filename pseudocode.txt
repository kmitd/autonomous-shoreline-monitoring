# pseudocode 

given : 
r = [img, timestamp,  location, sensor_type, event_type ] # new raw event  
E = []  # list of high-level events, initially empty

event_strategy_scripts = { event_type : script.py } # dictionary of event_types (face, computer, radio) and the strategy to invoke 

# match to kg
